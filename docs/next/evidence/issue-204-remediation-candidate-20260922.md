# Issue #204：候选修复状态（2026-09-22）

基线 `293c5662f0b7a4c6ca6928c4c7fdc1dc48518997`。本记录随候选源码一并应用，不代表已经合并、WDK 构建成功或硬件验收通过。

本轮改动：源叶必须覆盖整个区域，属性扫描不能绕过准入；拒绝路径先保存诊断再释放；A/D 记录绑定实际发布槽位；大叶还必须受 EPT01 粒度与视图限制。对细粒度来源的大页请求采取明确拒绝，不是已实现多页强租约。

Cross-View 改为 ID 查找取得引用再核对地址/类型，失败保留只读候选；进程 public walk、线程 public walk 和线程列表的进程 walk 使用“处理当前项后再推进”的引用转移顺序，提前退出释放当前游标。内部 PsGetNextProcess* 的目标构建行为仍需实际核验。

新增参数门阻止在验证前执行任意 requestedShift 的位移；NMI 父资源释放门增加 NmiWindow.Applied。后者仅为防御性完整性补强，TF/RF 观察窗口的架构透明性并未解决。

制作环境实际执行：Clang 17 / C11 / Werror / ASan / UBSan 的纯策略、mock 对象引用、三个真实源码循环片段和拒绝/槽位片段测试，以及合成 Git 仓库上的补丁工具测试。所有最终隔离目标通过。完整源码工作树应用、原有完整测试、WDK/API/CAT、Driver Verifier、真实 AMD/Intel VM entry/exit 均未在制作环境执行。

完整交付包包含 patchspec.json、身份锁定应用工具、测试源码及实际测试日志。不要把隔离测试计数当成硬件覆盖率；完整 L2 OS、并发内层 vCPU、长期常驻和最终停止卸载结论维持未验收。
